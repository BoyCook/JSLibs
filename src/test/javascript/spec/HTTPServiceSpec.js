describe('HTTPService', function(){
	var http;

    beforeEach(function(){
//		http = new HTTPService();
//
//		var error = function(r, status, e) {
//		    var msg = '';
//		    if (status == 'error') {
//		        var mapping = httpMappings.get(r.status);
//		        msg = 'Error (' + r.status + '): ' + (mapping != undefined ? mapping.value : '');
//		    } else {
//		        msg = 'Unknown Error: ' + r.status + ' - ' + status + ' - ' + r.statusText + ' - ' + e;
//		    }
//			alert(msg);
//		};
//
//		var xhr = function() {
//		    if (jQuery.browser.msie && jQuery.browser.version.substr(0, 1) <= 7){
//		        return new ActiveXObject('Microsoft.XMLHTTP');
//		    }  else {
//		        return new XMLHttpRequest();
//		    }
//		};
//
//		http.setup({
//			error: error,
//			xhr: xhr
//		});
    });

    it('should do get correctly', function(){
		// var url = 'http://bt.com/';
//		var url = '../jquery-ui-1.8.2.custom/index.html';
//		expect(http.requests.length).toEqual(0);
//		http.get(url, 'html');
//		expect(http.requests.length).toEqual(1);
//
//		var request = http.requests[0];
//		expect(request.url).toEqual(url);
//		expect(request.method).toEqual('GET');
//		expect(request.type).toEqual('html');
    });
});
